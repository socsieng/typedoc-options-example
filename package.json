{
  "name": "typedoc-options-example",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --project .",
    "example": "npm run build && typedoc --plugin $(pwd) --version",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typedoc": "^0.16.1"
  },
  "keywords": [
    "typedocplugin"
  ],
  "devDependencies": {
    "@types/node": "^13.1.6",
    "ts-node": "^8.6.1",
    "typescript": "^3.7.4"
  }
}
